{
  "name": "carel-commissioning-dashboard",
  "version": "1.0.0",
  "description": "CAREL Commissioning Dashboard - HVAC Control System",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "echo 'Build completed'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src/**/*.js",
    "clean": "rm -rf dist && mkdir dist",
    "serve": "node scripts/serve.js"
  },
  "keywords": ["hvac", "control", "dashboard", "carel", "commissioning"],
  "author": "CAREL Team",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-fetch": "^2.7.0",
    "@vercel/kv": "^1.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "eslint": "^9.22.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}